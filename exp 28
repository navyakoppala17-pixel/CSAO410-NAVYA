// Q28: Simulating grep command
#include <stdio.h>
#include <string.h>

int main()
{
    FILE *fp;
    char filename[100];
    char pattern[100];
    char line[500];
    int lineNumber = 0;

    printf("Enter filename: ");
    scanf("%s", filename);

    printf("Enter search pattern: ");
    scanf("%s", pattern);

    fp = fopen(filename, "r");

    if (fp == NULL)
    {
        perror("Unable to open file");
        return 1;
    }

    while (fgets(line, sizeof(line), fp) != NULL)
    {
        lineNumber++;

        if (strstr(line, pattern) != NULL)
        {
            printf("%d: %s", lineNumber, line);
        }
    }

    fclose(fp);

    return 0;
}
